{
  "name": "react-typescript-minimal",
  "version": "0.0.1",
  "description": "",
  "main": "./build/entry-point.js",
  "scripts": {
    "dev": "esbuild entry-point.tsx --bundle --watch --outdir=build --serve=3000 --serve-fallback=index.html",
    "build": "esbuild entry-point.tsx --bundle --outdir=build --serve-fallback=index.html",
    "test": "jest"
  },
  "author": "Saulo Castillos",
  "license": "MIT",
  "devDependencies": {
    "@testing-library/react": "^14.2.1",
    "@types/jest": "^29.5.12",
    "@types/react": "^18.2.55",
    "esbuild": "0.20.0",
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0",
    "ts-jest": "^29.1.2"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  }
}
